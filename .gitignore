# Zig build artifacts
.zig-cache/
zig-out/

# Golden-image diffs, written next to the goldens on a mismatch
src/gfx/testdata/golden/*.actual.png

# Editor and OS noise
.DS_Store
*.swp

# Claude / assistant state
.claude/

# Local mirror of fetched packages, recreated by the build
zig-pkg/
